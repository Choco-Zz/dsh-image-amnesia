# Contributing

1. `node --test` must stay green.
2. Do not persist stripped images back into the session log.
3. Keep `keepAtLeast >= 1` so native vision still receives the latest image.
4. Add a regression test for any new content-block shape.
